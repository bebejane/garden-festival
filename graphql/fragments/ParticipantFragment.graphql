#import "./ImageFragment.graphql"

fragment ParticipantFragment on ParticipantRecord {
  id
  slug
  title
  subTitle
   image {
      ...ImageFragment
  }
  summary(markdown: false)
  externalLinks {
    id
    url
    linkText
  }
}